@AGENTS.md

<!-- 各 CLI 专属补充只在此处添加，且必须有 AGENTS.md 中没有的对应内容。 -->

## Agent skills

### Issue tracker

Issues 通过 `gh` CLI 记录在 GitHub 仓库 ohwald/house-ops 的 Issues 中。See `docs/agents/issue-tracker.md`.

### Triage labels

五个标准 triage 角色，标签名与角色同名（`needs-triage` 等）。See `docs/agents/triage-labels.md`.

### Domain docs

single-context：根 `CONTEXT.md` + `docs/adr/`（按需惰性创建）。See `docs/agents/domain.md`.
